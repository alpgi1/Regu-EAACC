/**
 * Footer section — delegates to the adapted FooterSection ui component.
 */
import { FooterSection } from "@/components/ui/footer-section";

export default function Footer() {
  return <FooterSection />;
}
