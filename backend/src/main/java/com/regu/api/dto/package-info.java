/**
 * Request and response DTOs used by REST controllers.
 *
 * <p>Plain Java records (or classes) that define the HTTP API contract.
 * No JPA annotations — DTOs are mapped to/from domain objects in the controller layer.
 *
 * <p>Populated in Phase 6.
 */
package com.regu.api.dto;
