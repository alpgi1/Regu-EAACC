/**
 * REST controllers exposing the REGU backend over HTTP.
 *
 * <p>Each controller maps to a functional area (analysis submission, report retrieval,
 * health). Controllers delegate immediately to service/orchestration layer — no business
 * logic lives here.
 *
 * <p>Populated in Phase 6.
 */
package com.regu.api;
