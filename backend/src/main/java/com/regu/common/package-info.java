/**
 * Shared utilities, custom exceptions, and helper classes used across packages.
 *
 * <p>Provides cross-cutting concerns such as error types, string/date utilities,
 * and constants. Nothing in this package should depend on any other {@code com.regu}
 * sub-package to avoid circular dependencies.
 *
 * <p>Populated incrementally across phases.
 */
package com.regu.common;
