/**
 * Ingestion logic for curated EU AI Act use case examples.
 *
 * <p>Produces one chunk per scenario with rich metadata (risk category, sector, outcome)
 * so retrievers can surface relevant precedents alongside the legal text.
 *
 * <p>Populated in Phase 3.
 */
package com.regu.ingestion.usecase;
